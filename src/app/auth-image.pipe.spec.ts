import { AuthImagePipe } from './auth-image.pipe';

describe('AuthImagePipe', () => {
  it('create an instance', () => {
    const pipe = new AuthImagePipe(,0,0,0);
    expect(pipe).toBeTruthy();
  });
});
